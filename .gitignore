node_modules/
.next/
out/
build/
coverage/
*.tsbuildinfo
next-env.d.ts

.env
.env*.local
.env.production

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*

.vercel
supabase/.temp/
supabase/.branches/

# Delivery scratch: the bundle Claude writes into the folder and the
# PowerShell helper that consumes it. Local tooling, not part of the app.
*.bundle
ship.ps1
